{
  "name": "witnet-price-feed-examples",
  "version": "0.1.1",
  "description": "Price feed examples using the Witnet decentralized oracle network",
  "main": "",
  "repository": "https://github.com/witnet/witnet-pricefeed-examples",
  "author": "Witnet Devs <devs@witnet.io>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "console": "npx truffle console",
    "clean": "npm run clean:flattened && node ./scripts/clean.js && npm run postinstall",
    "clean:flattened": "node ./scripts/flattened-clean.js",
    "compile": "npm run compile:contracts",
    "compile:requests": "npx rad2sol",
    "compile:contracts": "npm run clean:flattened && npm run postinstall && npm run compile:requests && npx truffle compile --all",
    "coverage": "solidity-coverage",
    "flatten": "node ./scripts/flatten.js 2>&1",
    "fmt": "solhint --max-warnings 0 \"contracts/**/*.sol\" && eslint ./test && eslint ./scripts/*.js && solhint \"test/**/*.sol\"",
    "fmt!": "solhint --max-warnings 0 \"contracts/**/*.sol\" --fix && eslint ./test --fix && eslint ./scripts/*.js --fix && solhint \"test/**/*.sol\" --fix",
    "migrate": "npm run clean:flattened && npx truffle migrate --reset",
    "migrate:flattened": "node ./scripts/flattened-migrate.js 2>&1",
    "postinstall": "node ./scripts/postinstall.js 2>&1",
    "solium": "solium -d contracts",
    "solium:fix": "solium -d contracts --fix",
    "test": "npm run compile:requests && npx truffle test",
    "verify:flattened": "node ./scripts/flattened-verify.js 2>&1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.1.0",
    "ado-contracts": "^1.0.0",
    "openzeppelin-solidity": "^2.4.0",
    "witnet-ethereum-bridge": "^0.2.0",
    "witnet-requests": "^0.4.0"
  },
  "devDependencies": {
    "dotenv": "8.2.0",
    "eslint": "7.24.0",
    "eslint-config-standard": "16.0.2",
    "eslint-plugin-import": "2.22.1",
    "eslint-plugin-node": "11.1.0",
    "eslint-plugin-promise": "5.1.0",
    "eslint-plugin-standard": "5.0.0",
    "ganache-cli": "6.12.2",
    "node-eval": "2.0.0",
    "protocol-buffers": "4.2.0",
    "solhint": "3.3.4",
    "solidity-coverage": "0.7.16",
    "truffle": "5.3.1",
    "truffle-assertions": "0.9.2",
    "truffle-flattener": "witnet/truffle-flattener.git#single-experimental",
    "truffle-plugin-verify": "0.5.7",
    "truffle-verify": "1.0.8"
  }
}
